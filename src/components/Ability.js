
const Ability = ({
    ability
    }) => {
      return (
        <div className="w-72">
          <h2 className="text-violet-600 text-center">ABILITY :: {ability.name}</h2>
        </div>
      );
    };
    
    export default Ability;
    